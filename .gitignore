config_files/zshrc_env
config_files/gitconfig
config_files/stormy
.zshrc.local
.DS_Store
vim/.netrwhist
vim/undos/
.gemini
.serena

# Packaged skill bundles. The unpacked source directory is the tracked copy;
# the .skill zip is a build artifact that goes stale against it.
*.skill
